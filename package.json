{
  "name": "ci_testes_inteligentes",
  "version": "1.0.0",
  "description": "Pipeline de CI com testes manuais e com IA usando Playwright e SauceDemo",
  "scripts": {
    "test": "playwright test",
    "test:manual": "playwright test tests/manual/",
    "test:ai": "playwright test tests/ai/",
    "test:report": "playwright show-report",
    "lint": "eslint tests/**/*.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
